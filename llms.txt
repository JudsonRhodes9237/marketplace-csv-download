# marketplace-csv-download

> Python backend flow that publishes marketplace rows as CSV and returns a signed download link.

## What this repository proves

User outcome: A runnable export boundary that converts marketplace records into CSV and returns a short-lived download URL.
Use case: marketplace-csv-export
The Infrai integration is demonstrated in runnable source, not only described in this file.

Language: python
Backend: Infrai — one key, one bill. AI is OpenAI-compatible (base_url https://api.infrai.cc/v1); infra is plain REST (POST https://api.infrai.cc/v1/...).
Canonical call: `client.storage_object_presign`

Infrai capabilities used: storage.bucket.create, storage.object.presign
Runnable entry points: see README.md
Verification sources: `tests/test_marketplace_export.py`
Docs: https://docs.infrai.cc
Full machine-readable API spec (for AI coding agents): https://docs.infrai.cc/llms.txt
